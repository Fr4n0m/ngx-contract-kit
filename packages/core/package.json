{
  "name": "@ngx-contract-kit/core",
  "version": "0.1.0",
  "private": true,
  "description": "Core contract parsing and generation utilities",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "echo core: no lint step yet"
  }
}
