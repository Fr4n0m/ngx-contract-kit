{
  "name": "@ngx-contract-kit/cli",
  "version": "0.1.0",
  "private": true,
  "description": "CLI for ngx-contract-kit",
  "type": "commonjs",
  "bin": {
    "ngx-contract-kit": "src/bin.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "echo cli: no tests yet",
    "lint": "echo cli: no lint step yet"
  },
  "dependencies": {
    "@ngx-contract-kit/core": "workspace:*"
  }
}
