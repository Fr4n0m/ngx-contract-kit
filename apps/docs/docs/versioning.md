# Versioning

Use semantic versioning based on contract impact.

- patch: non-breaking internal improvements
- minor: backward-compatible additions
- major: breaking contract changes

Run `pnpm ckit:check` in CI before release.
