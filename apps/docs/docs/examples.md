# Examples

Recommended end-to-end example:

1. Create contract file in `contracts/`.
2. Run `pnpm ckit:generate`.
3. Use generated models in Angular services.
4. Validate changes with `pnpm ckit:check`.
